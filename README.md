# AI 绘画提示词超市 v2.9.0

**中文简体** | [**English**](./docs/en/README.md)

本地离线的 AI 绘画提示词生成工具：**点选中文标签，自动生成英文提示词**。单文件、零依赖、双击即用。

## 特性

- **全量标签库**：内置 31.7 万+ Danbooru 中英对照标签（含角色 / 画师 / 版权 / 作品分类），随版本过滤重分类更新
- **分类 + 二级分类**：发色 / 发型 / 眼睛 / 表情 / 身材 / 服装 / 姿势 / 场景 / 画风 / NSFW 等，大类可继续细分（如服装 → 上衣 / 裙装 / 内衣 / 鞋配饰）
- **全局搜索**：中文、英文、下划线、空格都能搜，覆盖全库
- **收藏**：★ 收藏常用词，支持「仅看收藏」筛选，跨会话持久化；**搜索结果可直接点 ☆ 收藏**
- **导入 / 导出收藏**：收藏可导出为 JSON 文件备份，也可从 JSON 文件导入合并；导出按字母排序，方便版本管理
- **自动保存**：开启后收藏每次变动自动写入指定 JSON 文件（内置记忆保留，文件为并行备份）；首次开启需在 Chrome/Edge 中手动选择一次保存位置，右键按钮可随时更换
- **全部浏览**：一键查看最常用 1000 条
- **负面提示词**：内置负面词分类，自动分流到负面框
- 下划线自动转空格输出（`long_hair` → `long hair`），贴进工作流即用
- **拖动排序**：提示词下方的中文小胶囊可直接拖动调整顺序
- **提示词回填识别**：正面 / 负面文本框可直接粘贴或输入已有提示词，自动识别成小胶囊（匹配数据库显示中文，未匹配保留原文并标 `?`）

## 用法

双击打开 `index.html`，点选分类中的中文标签，顶部的提示词栏会自动拼好英文提示词，一键复制正面 / 负面。

## 在线使用（GitHub Pages 托管）

本工具已通过 GitHub Pages 托管，无需下载即可在线使用：

**https://bfrkqsb7.github.io/ai-prompt-supermarket/**

> ⚠️ 文件较大（约 14MB），首次加载可能需要几秒到十几秒，请耐心等待。

- 在线版与本地版完全一致（同一份 `index.html`）
- 本地版仍可下载离线使用（数据全内联，双击即开）

## 适配模型

Stable Diffusion 系标签型模型：**NoobAI-XL / Illustrious / Manhwa Style / Pony Diffusion** 等（Danbooru 标签体系）。

## 相关工具

把中文需求自动转成英文提示词的 **AI 提示词工坊**（SDXL 标签流 / 自然语言流 / H3 视频提示词，内置直白词典 + 提示词库参考，可直接检索本超市的 31.7 万全量标签）：

👉 [AI 提示词工坊 — AI-Prompt-Studio](https://github.com/BFRKQSB7/AI-Prompt-Studio) · [在线版](https://bfrkqsb7.github.io/AI-Prompt-Studio/)

**什么时候该去**：想用中文一句话生成标签/提示词、或要 H3 视频提示词 → 去工坊；本超市是手动选词的标签库，适合精细挑词。

## 变更日志

### v2.9.0
- **更新**：全量库过滤重分类至 317,423 条（剔除噪声 / 版权重分类，保留原始中文与分类）
- **新增**：分类页底部居中「加载全部 N 条」按钮 —— 一键完整加载该分类全部标签（分块渲染，大分类不卡）
- **修复**：原库个别条目括号损坏（chubby）随重建修复

### v2.8.2
- **新增**：补充 10 个常用负面词条到全量库（类型 0，含中文翻译）——easynegative / low_quality / worst_quality / normal_quality / text / error / missing_fingers / extra_fingers / ugly / bad-hands（转换器可据此给负面胶囊补中文）

### v2.8.1
- **新增**：深浅色切换（工具栏 🌓 按钮，默认深色）
- **优化**：README 增加「相关工具」跳转链接（去写实提示词生成器）

### v2.8.0
- **新增**：预设功能 —— 只存一套（正面 + 负面），点「存预设」保存当前正负提示词，「载预设」恢复并自动重建胶囊，「清预设」清除；与收藏同存 `dtag_fav.json`（新格式 `{fav:[...], preset:{pos,neg}}`，兼容旧数组格式导入）

### v2.7.0
- **新增**：NoobAI 官方标签补齐 —— 新增「年代/日期」分类（old / early / mid / recent / newest 日期标签）；「质量词」补 `newest` `safe` `detailed face` `detailed background`
- **新增**：常用分类扩充约 44 个标签（角色数量 / 发型 / 眼睛 / 表情 / 身材 / 服装 / 姿势 / 场景 / 画风 / 负面词），全量库新增 6 个词（chubby / bunny suit / hand on chin / fingers crossed / noise / grainy）
- **修复**：提示词胶囊点击本体不再切换正负（只 `±` 按钮切换，左键仅用于拖拽排序）
- **优化**：文件顶部新增结构导航 + 维护手册注释，标明全量库巨行位置，便于 AI 读取与维护

### v2.6.0
- **新增**：提示词权重识别 —— 粘贴 `(标签:权重)` 自动识别标签并显示权重徽章；组权重 `(a, b:1.5)` 给组内每个标签加权；权重 1 不显示（等同无权重）
- **新增**：右键胶囊调权重 —— 弹层手动输入（只认阿拉伯数字，默认 1）+ `±0.1` 小箭头 + 重置按钮（一键重置为 1）
- 未匹配的带权重词保留原文（如 `(weird thing:1.3)` 原样保留）

### v2.5.0
- **新增**：提示词胶囊新增 `×` 删除按钮；`±` 移到胶囊最左侧（点击切换正负），悬停时才变红且只亮鼠标所在按钮，按钮显示手型指针
- **新增**：自动保存文件夹路径显示在按钮右侧（`文件夹名 / dtag_fav.json`），与按钮文字同一水平线
- **新增**：右键重选保存文件夹时，从上次保存的文件夹位置开始（`startIn`）

### v2.4.1
- **修复**：搜索框有字时点击其他按钮，搜索结果列表不再消失（仅清空搜索框或点选结果时才隐藏）

### v2.4.0
- **新增**：提示词回填识别 —— 正面 / 负面文本框可直接粘贴或输入提示词，自动识别成小胶囊（匹配数据库显示中文，未匹配保留原文并标 `?`）
- **删除**：移除 `index_old.html` 旧版备份（git 历史即备份，不再随仓库分发）

### v2.3.0
- **新增**：提示词下方的中文小胶囊可拖动调整顺序
- **版本**：页面标题改为「AI 绘画提示词超市 v2.3.0」

### v2.2.0
- **新增**：搜索结果可直接点 ☆ 收藏 / 取消收藏

### v2.1.0
- **新增**：导入收藏 —— 从 JSON 文件导入并合并收藏
- **新增**：导出收藏 —— 收藏导出为 JSON 文件（字母排序）
- **新增**：自动保存 —— 收藏变动自动写入指定 JSON 文件（需 Chrome/Edge，支持随时重选保存位置）
- **修复**：收藏分类中部分标签中文缺失（仅存在于精选分类、不在全量库的词会显示英文）
- **版本**：旧版备份为 `index_old.html`

## 说明

- 工具为单文件 HTML，全部数据内联，离线可用，体积约 14MB
- 标签库覆盖成人内容分类；请遵守当地法律与平台规范，勿生成未成年人内容
