# AI 绘画提示词超市 维护文档

## 项目概览与目标

本地离线的 AI 绘画提示词生成工具：点选中文标签自动生成英文提示词。单 HTML、零依赖、双击即用，内置 31.7 万+ Danbooru 中英对照标签（含角色/画师/版权/作品分类）。**边界**：只生成/组织提示词，不做生图推理、不依赖后端。

## 维护状态

个人维护项目，按需更新，PR 欢迎但不保证及时。单文件 HTML 内嵌全量库与逻辑，**数据更新是主旋律**（v2.9.0 做过全量库过滤重分类）。**改脚本前先看锚点位置**（`grep` 锚点列在下方代码地图）。

## 构建 / 运行 / 测试

- **运行**：双击 `index.html` 或静态服务器 / GitHub Pages 在线版
- **测试**：浏览器实测功能（搜索/收藏/预设/胶囊排序/回填识别），无自动化测试
- **数据**：全量库 `supermarket_full_tags_317423.json`（[en,zh,cat,count] 317,423 条）与 `index.html` 内嵌一致（是索引内嵌的完整复制品，改数据两处同步）

## 目录结构 / 代码地图

| 路径 | 职责 |
|------|------|
| `index.html` | 单文件应用（HTML+CSS+JS 内联 + 内嵌全量库索引） |
| `supermarket_full_tags_317423.json` | 全量库备份（与 index 内嵌一致，原样不动）|
| `docs/en/` | 英文 README |
| `LICENSE` | MIT |

**版本号 v2.9.0 出现 3 处（改版本必须 `grep v2.9.0` 全改，改完复查零残留）**：
1. `<title>AI 绘画提示词超市 v2.9.0</title>`（L6）
2. 顶部结构导航/版本标注（L9）
3. `<h1>AI 绘画提示词超市 v2.9.0</h1>`（L194）

## 发布流程（对齐 improve skill）

1. 改版本号：`grep vX.Y.Z` 全改上述 3 处，改完复查零残留
2. README「变更日志」节顶部加新版本段（分类词：新增/修复/删除/优化/版本），中英 README 同步
3. 若改数据：`supermarket_full_tags_317423.json` 与内嵌索引必须一致，README 相关计数（31.7 万等）同步
4. commit（作者 `226671264+BFRKQSB7@users.noreply.github.com`）→ tag `v{版本号}` → push → `gh release create`

## 提交规范

Conventional Commits 轻量 5 前缀：`feat` / `fix` / `docs` / `refactor` / `chore`。小更新+距上次不久可覆盖发布（重打同 tag）。

## 版本纪律

semver：破坏性→x+1、新功能→y+1、修 bug→z+1。tag 与 index.html 版本号同步。历史 tag：v2.1.0 ~ v2.9.0（12 个）。

## 本仓库特有坑 / 注意

- **README 曾残留过时计数**（16.7万 vs 31.7万）——改数据后相关工具/计数交叉引用区必须同步，别只改 index.html
- 数据源说明"每日更新"是误史，实为**随版本更新**（v2.9.0 过滤重分类），README 不能写回"每日更新"
- 写实生成器（相关工具）已废弃不列出；相关工具区引用必须核对现状
- GitHub Pages 在线版约 14MB 首次加载，README 已提示"加载慢需等待"，别删提示